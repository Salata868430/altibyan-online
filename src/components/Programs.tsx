import type { Course } from "@/lib/content";
export default function Programs({ courses, whatsappUrl }: { courses: Course[]; whatsappUrl: string }) { return <section id="programs" className="section-pad bg-white"><div className="container-page"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">الدورات المتاحة</p><h2 className="section-title mt-4">سبعة مسارات تعليمية</h2></div><ol className="mx-auto mt-12 grid max-w-5xl gap-4 md:grid-cols-2">{courses.map((course,index) => <li key={course.id ?? `${course.name}-${index}`} className={`flex items-center gap-5 rounded-2xl border border-slate-200 bg-slate-50 p-5 transition hover:border-sky-300 hover:bg-white hover:shadow-lg ${index === courses.length - 1 ? "md:col-span-2" : ""}`}><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-[#071a3b] font-black text-sky-300">{index + 1}</span><div><h3 className="font-black leading-7 text-[#071a3b]">{course.name}</h3>{course.description && <p className="mt-1 text-sm text-slate-600">{course.description}</p>}</div></li>)}</ol><div className="mt-10 text-center"><a href={whatsappUrl} target="_blank" rel="noreferrer" className="inline-flex rounded-xl bg-[#071a3b] px-7 py-4 font-black text-white transition hover:bg-sky-700">استفسر عن الدورة المناسبة عبر واتساب</a></div></div></section>; }
