import type { FeatureContent } from "@/lib/content";
export default function Features({ content }: { content: FeatureContent }) { return <section id="features" className="section-pad bg-slate-50"><div className="container-page"><div className="mx-auto max-w-2xl text-center"><p className="eyebrow">{content.eyebrow}</p><h2 className="section-title mt-4">{content.title}</h2></div><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{content.items.map((benefit, index) => <article key={`${benefit}-${index}`} className="flex min-h-28 items-center gap-4 rounded-2xl border border-slate-200 bg-white p-5"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-sky-100 font-black text-sky-700">{index + 1}</span><h3 className="font-black leading-7 text-[#071a3b]">{benefit}</h3></article>)}</div></div></section>; }
