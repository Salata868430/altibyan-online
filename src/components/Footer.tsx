import Image from "next/image";
import type { ContactContent, LinkItem, SettingsContent } from "@/lib/content";
export default function Footer({ links, settings, contact }: { links: LinkItem[]; settings: SettingsContent; contact: ContactContent }) { return <footer className="bg-[#041127] text-white"><div className="container-page grid gap-10 py-14 md:grid-cols-3"><div><a href="#home" className="inline-flex items-center"><Image src={settings.logo_url || "/logo/logo-full.svg"} alt={settings.site_name} width={160} height={46} unoptimized={settings.logo_url.startsWith("http")} className="h-11 w-auto object-contain brightness-0 invert"/></a><p className="mt-5 max-w-sm leading-8 text-slate-400">{settings.description}</p></div><nav aria-label="روابط التذييل"><h2 className="font-black text-sky-300">روابط سريعة</h2><ul className="mt-5 grid grid-cols-2 gap-3 text-sm text-slate-300">{links.map(link => <li key={`${link.href}-${link.label}`}><a className="hover:text-sky-300" href={link.href}>{link.label}</a></li>)}</ul></nav><div><h2 className="font-black text-sky-300">التواصل</h2><div className="mt-5 space-y-3 text-sm text-slate-300"><p>الهاتف وواتساب</p><a dir="ltr" className="inline-block text-lg font-bold text-white hover:text-sky-300" href={`https://wa.me/${contact.whatsapp.replace(/\D/g,"")}`} target="_blank" rel="noreferrer">{contact.phone}</a>{contact.email && <a href={`mailto:${contact.email}`} className="block">{contact.email}</a>}</div></div></div><div className="border-t border-white/10"><div className="container-page flex flex-col gap-3 py-6 text-sm text-slate-400 sm:flex-row sm:items-center sm:justify-between"><p>© {new Date().getFullYear()} {settings.site_name}. {settings.footer_text}</p><div className="flex gap-5"><a href="/admin/login" className="text-xs hover:text-sky-300">دخول الإدارة</a><a href="#home" className="hover:text-sky-300">العودة إلى الأعلى ↑</a></div></div></div></footer>; }
